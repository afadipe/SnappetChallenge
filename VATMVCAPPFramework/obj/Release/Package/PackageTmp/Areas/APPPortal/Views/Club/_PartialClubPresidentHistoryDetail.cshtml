
@model List<VATMVCAPPFramework.ViewModel.ClubPresidentHistoryViewModel>
@using VATMVCAPPFramework.Utilities


<table id="clubPresidentHistorytbl" class="table convert-data-table table-striped">
    <thead>
        <tr>
            <th>S/N</th>
            <th>Old President</th>
            <th>New President</th>
            <th>Effective Date</th>
        </tr>
    </thead>
    <tbody>
        @{int rowNo = 0;}
        @foreach (var item in Model)
        {
            <tr>
                <td>@(rowNo += 1)</td>
                <td>@Html.DisplayFor(modelItem => item.oldPresidentName)</td>
                <td>@Html.DisplayFor(modelItem => item.NewPresidentName)</td>
                <td>@Html.DisplayFor(modelItem => item.EffectiveDate)</td>
            </tr>
        }

    </tbody>
</table>
